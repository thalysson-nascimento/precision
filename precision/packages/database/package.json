{
  "name": "@precision/database",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "npm run db:generate:postgres",
    "db:generate:sqlite": "npx prisma generate --schema=schema.sqlite.prisma",
    "db:generate:postgres": "npx prisma generate --schema=schema.postgres.prisma",
    "db:push:sqlite": "npx prisma db push --schema=schema.sqlite.prisma",
    "db:migrate:postgres": "npx prisma migrate dev --schema=schema.postgres.prisma"
  },
  "dependencies": {
    "@prisma/client": "^7.8.0",
    "dotenv": "^16.4.5"
  },
  "devDependencies": {
    "@prisma/adapter-better-sqlite3": "^7.8.0",
    "@prisma/adapter-pg": "^7.8.0",
    "better-sqlite3": "^12.11.1",
    "pg": "^8.22.0",
    "prisma": "^7.8.0"
  }
}
